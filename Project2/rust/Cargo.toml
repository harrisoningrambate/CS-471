cargo-features = ["edition2024"]
[package]
name = "Project-2"
version = "0.1.0"
edition = "2024"

[dependencies]
cust = "0.3.2"
kernels = { path = "kernels"}

[build-dependencies]
rustc_codegen_nvvm = "0.3.0"
